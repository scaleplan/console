#!/usr/bin/env php
<?php

require_once $_SERVER['DOCUMENT_ROOT'] . 'vendor/autoload.php';

try {
    $command = Scaleplan\Console\CommandFabric::getCommand($argv);
    $command->run();
} catch (Scaleplan\Console\Exceptions\AbstractException $e) {
    echo $e->getMessage();
}
